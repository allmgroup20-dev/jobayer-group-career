{
  "name": "jgcareer-chat-worker",
  "version": "1.0.1",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "esbuild src/index.ts --bundle --outfile=dist/worker.js --format=esm --platform=neutral --external:__STATIC_CONTENT_MANIFEST --minify --sourcemap"
  },
  "dependencies": {}
}
